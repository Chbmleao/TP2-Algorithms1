#include <iostream>
#include <vector>

class Solution {
    public:
        Solution();
        float sum;
        float maxSubarraySum;
        float suffix;
        float prefix;
};

std::vector<float> createTotalRatingVector(int numRows, int numCols);
void readFile();
